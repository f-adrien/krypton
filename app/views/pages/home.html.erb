<h1>Find your SuperHero !</h1>
<div class="container">
  <% @superheros.each do |hero| %>
  <%= hero.name %>
  <%= hero.superpower %>
  <%= hero.location %>
  <p><%= hero.price_per_day %> €</p>
  <% end %>
</div>

